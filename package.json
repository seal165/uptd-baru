{
  "name": "uptd-lab",
  "version": "2.0.0",
  "description": "UPTD Lab Pengujian - monorepo root",
  "scripts": {
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "start": "npm run start --prefix backend & npm run start --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "axios": "^1.18.1",
    "form-data": "^4.0.5",
    "fs-extra": "^11.3.5",
    "lodash": "^4.18.1",
    "mysqldump": "^3.2.0",
    "nodemailer": "^9.0.1",
    "prisma": "^6.19.3"
  }
}
